<?xml version="1.0" encoding="UTF-8"?>
<!--

     SPDX-License-Identifier: Apache-2.0

     Copyright The original authors

     Licensed under the Apache Software License version 2.0, available at http://www.apache.org/licenses/LICENSE-2.0

-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>dev.hardwood</groupId>
    <artifactId>hardwood-parent</artifactId>
    <version>1.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>hardwood-core</artifactId>

  <name>Hardwood Core</name>
  <description>Core Parquet reader implementation</description>
  <url>https://hardwood.dev/</url>

  <scm>
    <connection>scm:git:git://github.com/hardwood-hq/hardwood.git</connection>
    <developerConnection>scm:git:git@github.com:hardwood-hq/hardwood.git</developerConnection>
    <url>https://github.com/hardwood-hq/hardwood/</url>
    <tag>HEAD</tag>
  </scm>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>dev.hardwood</groupId>
        <artifactId>hardwood-bom</artifactId>
        <version>${project.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>dev.hardwood</groupId>
        <artifactId>hardwood-test-bom</artifactId>
        <version>${project.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <!-- Not used by any code here. Declared so the reactor builds the Error Prone checks
         before this module compiles: they reach javac through the compiler plugin's
         annotationProcessorPaths, which the reactor does not read when ordering modules,
         so under `-T` there is otherwise nothing stopping the two running at once. -->
    <dependency>
      <groupId>dev.hardwood</groupId>
      <artifactId>hardwood-error-prone-checks</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>

    <!-- Test dependencies -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.logging.log4j</groupId>
      <artifactId>log4j-api</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.logging.log4j</groupId>
      <artifactId>log4j-core</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.logging.log4j</groupId>
      <artifactId>log4j-jpl</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.locationtech.jts</groupId>
      <artifactId>jts-core</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>dev.hardwood</groupId>
      <artifactId>hardwood-test-support</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.duckdb</groupId>
      <artifactId>duckdb_jdbc</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- parquet-java (reference implementation) — oracle for bloom-filter hashing/membership -->
    <dependency>
      <groupId>org.apache.parquet</groupId>
      <artifactId>parquet-column</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Thrift structs (BloomFilterHeader/Util) — to serialize a legacy filter fixture in tests -->
    <dependency>
      <groupId>org.apache.parquet</groupId>
      <artifactId>parquet-format-structures</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- Optional compression libraries -->
    <dependency>
      <groupId>org.xerial.snappy</groupId>
      <artifactId>snappy-java</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>com.github.luben</groupId>
      <artifactId>zstd-jni</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>at.yawk.lz4</groupId>
      <artifactId>lz4-java</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>com.aayushatharva.brotli4j</groupId>
      <artifactId>brotli4j</artifactId>
      <optional>true</optional>
    </dependency>
  </dependencies>

  <build>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
        <filtering>false</filtering>
      </resource>
      <!-- Filtering is a per-resource flag, so anything needing build properties
           substituted lives in its own root rather than behind include globs on the
           one above. Keeps token substitution away from the native-image configs. -->
      <resource>
        <directory>src/main/resources-filtered</directory>
        <filtering>true</filtering>
      </resource>
    </resources>
    <plugins>
      <!-- Compile Java 22+ specific code for FFM API -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <executions>
          <execution>
            <id>compile-java22</id>
            <phase>compile</phase>
            <goals>
              <goal>compile</goal>
            </goals>
            <configuration>
              <release>22</release>
              <compileSourceRoots>
                <compileSourceRoot>${project.basedir}/src/main/java22</compileSourceRoot>
              </compileSourceRoots>
              <multiReleaseOutput>true</multiReleaseOutput>
              <compilerArgs combine.children="append">
                <arg>--add-modules</arg>
                <arg>jdk.incubator.vector</arg>
              </compilerArgs>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Run unit tests with Vector API module -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <argLine>--add-modules jdk.incubator.vector</argLine>
        </configuration>
      </plugin>

      <!-- Create multi-release JAR. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <configuration>
          <archive>
            <manifestEntries>
              <Multi-Release>true</Multi-Release>
              <Automatic-Module-Name>dev.hardwood.core</Automatic-Module-Name>
            </manifestEntries>
          </archive>
        </configuration>
      </plugin>

      <!-- Run integration tests against the packaged JAR to test multi-release classes -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <configuration>
          <classesDirectory>${project.build.directory}/${project.build.finalName}.jar</classesDirectory>
          <argLine>--enable-native-access=ALL-UNNAMED --add-modules jdk.incubator.vector</argLine>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!-- Mutation analysis over the bloom filter I/O classes. Opt-in because it recompiles
         and re-runs the bloom suites once per planted fault: `./mvnw -pl core
         -Pmutation-test test-compile org.pitest:pitest-maven:mutationCoverage`. -->
    <profile>
      <id>mutation-test</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.pitest</groupId>
            <artifactId>pitest-maven</artifactId>
            <configuration>
              <mutationThreshold>89</mutationThreshold>
              <maxSurviving>13</maxSurviving>
              <targetClasses>
                <param>dev.hardwood.internal.predicate.BloomFilterPrefetcher</param>
                <param>dev.hardwood.internal.predicate.BloomFilterReadPlanner</param>
                <param>dev.hardwood.internal.predicate.BloomFilterProbe</param>
                <param>dev.hardwood.internal.predicate.BloomFilterPrefetch*</param>
                <param>dev.hardwood.internal.predicate.RowGroupBloomFilterSource</param>
              </targetClasses>
              <targetTests>
                <param>dev.hardwood.internal.predicate.BloomFilterIoCoalescingTest</param>
                <param>dev.hardwood.internal.predicate.BloomFilterReadPlannerTest</param>
                <param>dev.hardwood.internal.predicate.BloomFilterPushDownTest</param>
                <param>dev.hardwood.internal.predicate.BloomFilterSignedZeroPushDownTest</param>
                <param>dev.hardwood.BloomFilterEndToEndTest</param>
              </targetTests>
              <outputFormats>
                <param>HTML</param>
                <param>CSV</param>
              </outputFormats>
              <timestampedReports>false</timestampedReports>
              <jvmArgs>
                <value>--add-modules</value>
                <value>jdk.incubator.vector</value>
              </jvmArgs>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
