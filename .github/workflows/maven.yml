#
#  Copyright 2021 The original authors
#
#  Licensed under the Apache License, Version 2.0 (the "License");
#  you may not use this file except in compliance with the License.
#  You may obtain a copy of the License at
#
#      http://www.apache.org/licenses/LICENSE-2.0
#
#  Unless required by applicable law or agreed to in writing, software
#  distributed under the License is distributed on an "AS IS" BASIS,
#  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
#  See the License for the specific language governing permissions and
#  limitations under the License.
#

name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 'Check out repository'
        uses: actions/checkout@v4
        with:
          submodules: 'true'

      - name: 'Set up Java'
        uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: 'temurin'

      - name: 'Cache Maven packages'
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      - name: 'Build and install modules'
        run: ./mvnw -B clean install -DskipTests

      - name: 'Upload built artifacts'
        uses: actions/upload-artifact@v4
        with:
          name: maven-repo
          path: ~/.m2/repository/dev/hardwoodhq/
          retention-days: 1

  integration-tests:
    needs: build
    runs-on: ubuntu-latest

    strategy:
      matrix:
        include:
          - java: '21'
            preview: false
            install_libdeflate: false
            name: 'Java 21'
          - java: '25'
            preview: false
            install_libdeflate: true
            name: 'Java 25'
          - java: '25'
            preview: true
            install_libdeflate: true
            name: 'Java 25 (preview)'

    name: Integration Tests - ${{ matrix.name }}

    steps:
      - name: 'Check out repository'
        uses: actions/checkout@v4
        with:
          submodules: 'true'

      - name: 'Set up Java'
        uses: actions/setup-java@v5
        with:
          java-version: ${{ matrix.java }}
          distribution: 'temurin'

      - name: 'Install libdeflate'
        if: ${{ matrix.install_libdeflate }}
        run: sudo apt-get update && sudo apt-get install -y libdeflate-dev

      - name: 'Cache Maven packages'
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      - name: 'Download built artifacts'
        uses: actions/download-artifact@v4
        with:
          name: maven-repo
          path: ~/.m2/repository/dev/hardwoodhq/

      - name: 'Run integration tests'
        if: ${{ !matrix.preview && !matrix.install_libdeflate }}
        run: ./mvnw -B verify -pl :hardwood-integration-test

      - name: 'Run integration tests (with libdeflate)'
        if: ${{ !matrix.preview && matrix.install_libdeflate }}
        run: ./mvnw -B verify -pl :hardwood-integration-test -Dlibdeflate.required=true

      - name: 'Run integration tests (preview features)'
        if: ${{ matrix.preview }}
        run: ./mvnw -B verify -pl :hardwood-integration-test -Dlibdeflate.required=true -DargLine="--enable-native-access=ALL-UNNAMED --enable-preview"

  performance-tests:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: 'Check out repository'
        uses: actions/checkout@v4
        with:
          submodules: 'true'

      - name: 'Set up Java'
        uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: 'temurin'

      - name: 'Cache Maven packages'
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      - name: 'Download built artifacts'
        uses: actions/download-artifact@v4
        with:
          name: maven-repo
          path: ~/.m2/repository/dev/hardwoodhq/

      - name: 'Cache taxi data'
        uses: actions/cache@v4
        with:
          path: performance-testing/test-data-setup/target/tlc-trip-record-data/
          key: taxi-data-2023-01-to-2025-12

      - name: 'Run performance tests'
        run: ./mvnw -B verify -Pperformance-test -pl :hardwood-performance-testing -amd -Dperf.start=2023-01 -Dperf.end=2025-12
